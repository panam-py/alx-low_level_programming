#include "main.h"

/**
 * _abs - Returns the absolut value of a number
 * @i: The number to be entered
 * Return: int, The absolute value of number entered
*/
int _abs(int i)
{
if (i > 0)
return (i);
else
return (i * -1);
}
