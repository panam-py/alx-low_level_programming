#include "main.h"

/**
 * print_last_digit - Returns the last digit of an integer
 * @i: The integer passed
 * Return: int, the last digit in number
*/
int print_last_digit(int i)
{
return (i % 10);
}